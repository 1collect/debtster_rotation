services:
  rotation-go:
    build: .
    network_mode: host
    env_file:
      - .env
    environment:
      - ADDR=:8040
      - SERVICE_TOKEN=${SERVICE_TOKEN:-debtster-rotation}
    restart: unless-stopped
